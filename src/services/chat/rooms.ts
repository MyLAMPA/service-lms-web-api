
import * as _ from 'lodash'
import * as momnet from 'moment'

import {
    State,
} from '../../models'
import {
    Room,
} from '../../types/chat'

export async function getOrCreateRoomByCtx(ctx: string, state: State): Promise<Room> {
    return {}
}
